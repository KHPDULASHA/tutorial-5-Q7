#include <stdio.h>

int main()
{
   float x;
   int y;

   printf("Enter the value for x:");
   scanf("%f",&x);

   if (x<0)
   {
       printf("y is: %d",y=1);

   }
   else if (x==0)
   {
        printf("y is: %d",y=0);
   }
   else if (x>0)
   {
        printf("y is: %d",y=-1);
   }
    return ;






}
